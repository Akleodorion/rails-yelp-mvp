<h1>Add a new restaurant</h1>
<p>Please complete this form to add a restaurant</p>

<%= simple_form_for @restaurant do |f| %>
  <%= f.input :name %>
  <%= f.input :address %>
  <%= f.input :phone_number %>
  <%= f.input :category %>
  <%= f.submit "Add restaurant", class: "btn btn-primary" %>
<% end %>

<%= link_to "Retourner à l'index", restaurants_path  %>
